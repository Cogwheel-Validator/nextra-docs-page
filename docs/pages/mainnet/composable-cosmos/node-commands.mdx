---
DENOM: "ppica"
CHAIN_ID: "centauri-1"
BINARY: "centaurid"
CHAIN_NAME: "Composable Finance - Composable Cosmos chain"
CHAIN_HOME: "$HOME/.banksy"
DIRECTORY: "composable-centauri"
---
import { Callout } from 'nextra/components'

# Usefull commands for {$CHAIN_NAME}

## 🔑 Key management


<Callout type="info" emoji="👛">
  Change wallet in the commands to your own wallet name.
</Callout>

#### Add new key

```bash
{$BINARY} keys add wallet
```

#### Recover existing key

```bash
{$BINARY} keys add wallet --recover
```

#### List all keys

```bash
{$BINARY} keys list
```

#### Delete key

```bash
{$BINARY} keys delete wallet
```

#### Export key to the file

```bash
{$BINARY} keys export wallet
```

#### Import key from the file

```bash
{$BINARY} keys import wallet wallet.backup
```

#### Query wallet balance

```bash
{$BINARY} q bank balances $( {$BINARY} keys show wallet -a)
```

## 🧙 Validator management



<Callout type="info" emoji="🙋‍♀️">
  Make sure to change values for moniker, identity, details website to your own values.
</Callout>

#### Edit existing validator

```bash
{$BINARY} tx staking edit-validator \
--new-moniker "YOUR_MONIKER_NAME" \
--identity "YOUR_KEYBASE_ID" \
--details "YOUR_DETAILS" \
--website "YOUR_WEBSITE_URL" \
--chain-id {$CHAIN_ID} \
--commission-rate 0.05 \
--from wallet \
--gas-adjustment 1.5 \
--gas auto \
--gas-prices 0{$DENOM} \
-y
```

#### Unjail validator

```bash
{$BINARY} tx slashing unjail --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto
 --gas-prices 0{$DENOM} -y
```

#### Jail reason

```bash
{$BINARY} query slashing signing-info $( {$BINARY} tendermint show-validator)
```

#### List all active validators

```bash
{$BINARY} q staking validators -oj --limit=3000 | jq '.validators[] | select(.status=="BOND_STATUS_BONDED")' | jq -r '(.tokens|tonumber/pow(10; 6)|floor|tostring) + " \t " + .description.moniker' | sort -gr | nl
```

#### List all inactive validators

```bash
{$BINARY} q staking validators -oj --limit=3000 | jq '.validators[] | select(.status=="BOND_STATUS_UNBONDED")' | jq -r '(.tokens|tonumber/pow(10; 6)|floor|tostring) + " \t " + .description.moniker' | sort -gr | nl
```

#### View validator details

```bash
{$BINARY} q staking validator $( {$BINARY} keys show wallet --bech val -a)
```

## 🪙 Token management

#### Withdraw rewards from all validators

```bash
{$BINARY} tx distribution withdraw-all-rewards --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

#### Withdraw commission and rewards from your validator

```bash
{$BINARY} tx distribution withdraw-rewards $( {$BINARY} keys show wallet --bech val -a) --commission --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

#### Delegate tokens to yourself

```bash
{$BINARY} tx staking delegate $( {$BINARY} keys show wallet --bech val -a) 1000000000000{$DENOM} --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

#### Delegate tokens to validator

```bash
{$BINARY} tx staking delegate <TO_VALOPER_ADDRESS> 1000000000000{$DENOM} --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM}  -y
```

#### Redelegate tokens to another validator

```bash
{$BINARY} tx staking redelegate $( {$BINARY} keys show wallet --bech val -a) <TO_VALOPER_ADDRESS> 1000000${CHAIN_DENOM} --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

#### Unbond tokens from your validator

```bash
{$BINARY} tx staking unbond $( {$BINARY} keys show wallet --bech val -a) 1000000${CHAIN_DENOM} --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

#### Send tokens to the wallet

```bash
{$BINARY} tx bank send wallet <TO_WALLET_ADDRESS> 1000000${CHAIN_DENOM} --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

## 🏛️ Governance

#### List all proposals

```bash
{$BINARY} query gov proposals
```

#### View proposal by id

```bash
{$BINARY} query gov proposal 1
```

#### Vote 'Yes'

```bash
{$BINARY} tx gov vote 1 yes --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

#### Vote 'No'

```bash
{$BINARY} tx gov vote 1 no --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

#### Vote 'Abstain'

```bash
{$BINARY} tx gov vote 1 abstain --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

#### Vote 'NoWithVeto'

```bash
{$BINARY} tx gov vote 1 NoWithVeto --from wallet --chain-id {$CHAIN_ID} --gas-adjustment 1.5 --gas auto --gas-prices 0{$DENOM} -y
```

## ⚡️ Utility

#### Update Indexer

**Disable indexer**

```bash
sed -i -e 's|^indexer *=.*|indexer = "null"|' {$CHAIN_HOME}/config/config.toml
```

**Enable indexer**

```bash
sed -i -e 's|^indexer *=.*|indexer = "kv"|' {$CHAIN_HOME}/config/config.toml
```

#### Update pruning

```bash
sed -i \
  -e 's|^pruning *=.*|pruning = "custom"|' \
  -e 's|^pruning-keep-recent *=.*|pruning-keep-recent = "2000"|' \
  -e 's|^pruning-keep-every *=.*|pruning-keep-every = "0"|' \
  -e 's|^pruning-interval *=.*|pruning-interval = "100"|' \
  {$CHAIN_HOME}/config/app.toml
```

## 🔧 Maintenance

#### Get validator info

```bash
{$BINARY} status 2>&1 | jq .ValidatorInfo
```

#### Get sync info

```bash
{$BINARY} status 2>&1 | jq .SyncInfo
```

#### Enable prometheus

```bash
sed -i -e "s/prometheus = false/prometheus = true/" {$CHAIN_HOME}/config/config.toml
```

#### Reset chain data

```bash
{$BINARY} tendermint unsafe-reset-all --keep-addr-book --home {$CHAIN_HOME} --keep-addr-book
```

#### Remove node

<Callout type="error" emoji="☢️">
  Make sure to change values for moniker, identity, details website to your own values.
</Callout>

```bash
cd $HOME
sudo systemctl stop {$BINARY}
sudo systemctl disable {$BINARY}
sudo rm /etc/systemd/system/{$BINARY}.service
sudo systemctl daemon-reload
rm -f $(which {$BINARY})
rm -rf {$CHAIN_HOME}
rm -rf $HOME/{$DIRECTORY}
```

## ⚙️ Service Management

#### Reload service configuration

```bash
sudo systemctl daemon-reload
```

#### Enable service

```bash
sudo systemctl enable {$BINARY}
```

#### Disable service

```bash
sudo systemctl disable {$BINARY}
```

#### Start service

```bash
sudo systemctl start {$BINARY}
```

#### Stop service

```bash
sudo systemctl stop {$BINARY}
```

#### Restart service

```bash
sudo systemctl restart {$BINARY}
```

#### Check service status

```bash
sudo systemctl status {$BINARY}
```

#### Check service logs

```bash
sudo journalctl -u {$BINARY} -f --no-hostname -o cat
```